%model
%this is a simple example of a model in polar
%coordinates (radius r and angle theta). You can use
%the command makecartesian to change to normal coordinates
%
r'=-rr*r
theta'=k
%commands
k=3;
rr=0.01;
